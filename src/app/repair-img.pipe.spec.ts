import { RepairImgPipe } from './repair-img.pipe';

describe('RepairImgPipe', () => {
  it('create an instance', () => {
    const pipe = new RepairImgPipe();
    expect(pipe).toBeTruthy();
  });
});
